logging:
  level:
    root: info
    win.doyto.query: debug

spring:
  http:
    encoding:
      enabled: true
      charset: UTF-8
      force: true

  cache:
    type: simple

#兼容缓存的继承方式和配置方式
#doyto:
#  query:
#    caches:
#      module:user,
#      module:menu
