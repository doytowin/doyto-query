spring:
  datasource:
    driver-class-name: org.sqlite.JDBC
    url: jdbc:sqlite::resource:doytowin.db?date_string_format=yyyy-MM-dd'T'HH:mm:ss.SSS

doyto:
  query:
    config:
      dialect: win.doyto.query.dialect.SQLiteDialect
